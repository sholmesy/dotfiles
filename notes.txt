sudo pacman -S \
    firefox \
    xclip \
    git \
    urxvt-perls \
    fzf \
    neofetch \
    autojump \
    docker \
    docker-compose \
    bat \
    ripgrep \
    ranger \
    gvim \

aurman -S rofi-dmenu

ssh-keygen
cat .ssh/id_rsa.pub | xclip -selection clipboard
git clone https://github.com/sholmesy/dotfiles ~

rm ~/.bash*
ln -s ~/dotfiles/.bash* ~

ln -s ~/dotfiles/.vimrc ~
ln -s ~/dotfiles/.gitignore_global ~
ln -s ~/dotfiles/.inputrc ~

rm ~/.xinitrc
rm ~/.Xresources
rm ~/.Xmodmap
ln -s ~/dotfiles/.xinitrc ~
ln -s ~/dotfiles/.Xresources ~
ln -s ~/dotfiles/.Xmodmap ~

rm ~/.config/i3/config
cat ~/dotfiles/i3/config > ~/.config/i3/config
sudo vim /usr/share/x11/xkb/symbols/us

# Only on intel gpus
# sudo cat ~/dotfiles/intel-tear-free.conf > /etc/X11/xorg.conf.d/20-intel.conf
